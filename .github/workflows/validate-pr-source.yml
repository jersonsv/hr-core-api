name: Validate GitFlow PR Rules
on:
  pull_request:
    branches: [ main, develop ]  # Escuchar PRs hacia main Y develop
    types: [opened, synchronize, reopened]

jobs:
  validate-gitflow:
    runs-on: ubuntu-latest
    name: Validate GitFlow PR Rules
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      
    - name: Get Fresh PR Info
      id: fresh_pr
      uses: actions/github-script@v7
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          // Obtener información fresca del PR
          const { data: pr } = await github.rest.pulls.get({
            owner: context.repo.owner,
            repo: context.repo.repo,
            pull_number: context.issue.number
          });
          
          console.log('=== FRESH PR INFO ===');
          console.log('PR Number:', pr.number);
          console.log('PR Title:', pr.title);
          console.log('Head Branch:', pr.head.ref);
          console.log('Base Branch:', pr.base.ref);
          console.log('PR State:', pr.state);
          console.log('PR URL:', pr.html_url);
          console.log('===================');
          
          return {
            head_ref: pr.head.ref,
            base_ref: pr.base.ref,
            pr_number: pr.number,
            pr_state: pr.state
          };
    
    - name: Debug Context
      run: |
        echo "=== GITHUB CONTEXT DEBUG ==="
        echo "github.base_ref: '${{ github.base_ref }}'"
        echo "github.head_ref: '${{ github.head_ref }}'"
        echo "github.event.pull_request.base.ref: '${{ github.event.pull_request.base.ref }}'"
        echo "github.event.pull_request.head.ref: '${{ github.event.pull_request.head.ref }}'"
        echo ""
        echo "=== FRESH API DATA ==="
        echo "API Head Branch: ${{ fromJSON(steps.fresh_pr.outputs.result).head_ref }}"
        echo "API Base Branch: ${{ fromJSON(steps.fresh_pr.outputs.result).base_ref }}"
        echo "=========================="
        
    - name: Validate GitFlow Rules
      run: |
        FRESH_BASE="${{ fromJSON(steps.fresh_pr.outputs.result).base_ref }}"
        FRESH_HEAD="${{ fromJSON(steps.fresh_pr.outputs.result).head_ref }}"
        
        echo "🎯 Validating GitFlow Rules"
        echo "Target Branch: $FRESH_BASE"
        echo "Source Branch: $FRESH_HEAD"
        echo ""
        
        # Función para verificar si es una rama feature
        is_feature_branch() {
          local branch=$1
          if [[ $branch == feature/* ]] || [[ $branch =~ ^feature- ]]; then
            return 0  # true
          else
            return 1  # false
          fi
        }
        
        # REGLA 1: PRs hacia main solo pueden venir de develop
        if [ "$FRESH_BASE" = "main" ]; then
          echo "📋 REGLA: PRs hacia 'main' solo desde 'develop'"
          
          if [ "$FRESH_HEAD" != "develop" ]; then
            echo ""
            echo "❌ ERROR: Pull requests hacia 'main' solo pueden venir de 'develop'!"
            echo "❌ Fuente actual: $FRESH_HEAD"
            echo "❌ Fuente requerida: develop"
            echo ""
            echo "🔄 Proceso GitFlow correcto:"
            echo "1. Cerrar este PR"
            echo "2. Crear: $FRESH_HEAD → develop"
            echo "3. Después del merge a develop"
            echo "4. Crear: develop → main"
            echo ""
            exit 1
          fi
          
          echo "✅ SUCCESS: develop → main es válido!"
          echo "✅ Sigue el patrón GitFlow correcto"
          echo ""
        
        # REGLA 2: PRs hacia develop pueden venir de feature/* o hotfix/*
        elif [ "$FRESH_BASE" = "develop" ]; then
          echo "📋 REGLA: PRs hacia 'develop' desde feature/* o hotfix/*"
          
          # Verificar si viene de una rama válida
          if is_feature_branch "$FRESH_HEAD" || [[ $FRESH_HEAD == hotfix/* ]] || [[ $FRESH_HEAD =~ ^hotfix- ]]; then
            echo "✅ SUCCESS: $FRESH_HEAD → develop es válido!"
            echo "✅ Fuente válida para develop"
            echo ""
          else
            echo ""
            echo "❌ ERROR: Pull requests hacia 'develop' deben venir de ramas feature/* o hotfix/*"
            echo "❌ Fuente actual: $FRESH_HEAD"
            echo "❌ Fuentes válidas: feature/*, hotfix/*"
            echo ""
            echo "🔄 Proceso sugerido:"
            echo "1. Renombrar rama a: feature/$FRESH_HEAD"
            echo "2. O crear nueva rama feature desde $FRESH_HEAD"
            echo ""
            exit 1
          fi
        
        # REGLA 3: Otras ramas target (si las hay)
        else
          echo "📋 INFO: PR hacia rama '$FRESH_BASE'"
          echo "✅ No hay restricciones GitFlow para esta rama"
          echo ""
        fi
        
        echo "🎉 Validación GitFlow completada exitosamente!"
